# OAuth 2.0 Web client ID from Google Cloud Console (must match backend GOOGLE_CLIENT_ID).
# Add http://localhost:5173 to the client's "Authorized JavaScript origins".
VITE_GOOGLE_CLIENT_ID=

# API base URL; defaults to /api/v1 (proxied to the backend by Vite in development).
VITE_API_URL=
